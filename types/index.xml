<types namespace="_depack">
  <!-- import name="VNode" from="@externs/preact" ns="preact" /> -->

  <type record name="RenderConfig" desc="Rendering options.">
    <prop boolean name="addDoctype" default="false">
      Adds the `<!doctype html>` at the beginning of the return string.
    </prop>
    <prop boolean name="shallow" default="false">
      If `true`, renders nested Components as HTML elements (`<Foo a="b" />`).
    </prop>
    <prop boolean name="xml" default="false">
      If `true`, uses self-closing tags for elements without children.
    </prop>
    <prop boolean name="pretty" default="false">
      If `true`, adds `  ` whitespace for readability. Pass a string to indicate the indentation character, e.g., `\t`.
    </prop>
    <prop number name="lineLength" default="40">
      The number of characters on one line above which the line should be split in the `pretty` mode.
    </prop>
    <prop boolean name="closeVoidTags" default="false">
      Whether the void tags will be auto-closed (for xhtml support).
    </prop>
    <prop boolean name="renderRootComponent" default="false">
      When shallow rendering is on, will render root component.
    </prop>
  </type>
</types>